syntax = "proto3";

package image;

service ImageService {
    rpc ProcessIntent(ImageRequest) returns (ImageResponse);
}

message ImageRequest {
    string model = 1;

    string input = 2;
}

message ImageResponse {
    repeated string img_urls = 1;
}
